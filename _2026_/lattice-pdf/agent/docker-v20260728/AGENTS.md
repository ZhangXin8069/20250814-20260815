# AGENTS.md — agent/docker-v20260728

**历史版本——已被 `../docker-v20260802/` 取代。** CPU 版本，改进有效质量提取。

| 特性 | 值 |
|---|---|
| 计算引擎 | CPU（NumPy） |
| 有效质量 | **仅前向平均 + log 方法**（修复边界反射污染） |

关键改进：放弃朴素 arccosh，改用**仅前向平均**（避免边界反射污染）+ **log 有效质量**（对噪声重子更稳健）。新增诊断 `C2pt_1d_fwd`、`meff_log`、`meff_cosh`。该修复延续到 v20260729 及以后。**不要用于新工作。**

# AGENTS.md — agent/docker-v20260801

**历史版本——已被 `../docker-v20260802/` 取代。** 最终合并前功能最全的版本：自动平台有效质量 + Pz=0 校准。

| 特性 | 值 |
|---|---|
| 有效质量 | **自动平台 exp_forward**（自动剔除激发态污染） |
| Pz 校准 | **Pz=0 + Pz=-2**（静止质量校准） |
| 文件集 | 最少（7 文件，无 README/check_env/diagnose） |

## 关键创新

1. **自动平台有效质量**——`exp_forward` + 自动平台检测，替代硬编码拟合窗
2. **Pz=0 校准**——同时算静止系（Pz=0）与助推系（Pz=-2）关联函数，先校准静止质量，再验证助推有效质量符合 E = √(m² + P²)

**不要用于新工作。**

# AGENTS.md — docker

容器化部署占位目录（当前仅 `PASS` 占位文件）。

现行 GPU 流水线在 `agent/docker-v*/`（版本化快照），launcher 为 `agent/docker/run_gpu_pipeline.sh`。当前活跃版本见 `agent/docker-v20260802/`（经典参考）与 `agent/docker-v20260805/`（现行全量工具包）。
